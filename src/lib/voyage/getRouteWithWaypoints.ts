// Mock waypoints - replace with actual data source when backend is ready

export async function getRouteWaypoints(routeId: string) {
  console.log('Mock getRouteWaypoints for routeId:', routeId);
  return [];
}